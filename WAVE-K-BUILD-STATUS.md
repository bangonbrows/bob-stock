# Wave K (Tier 4) — Build Status (overnight 2026-06-17)

**Branch:** `fix/cli-tri-audit-2026-05` · **Wave J committed** `30d6d6e` · **This wave: NOT committed** (in progress, for Kunal's review).
**Locked plan:** `audit-artifacts/WAVE-K-TIER4-SCOPE.md` → "★ CONSOLIDATED FINAL PLAN" (both auditors approved).

## What I built tonight (deliberately scoped — safe, additive, proven)

I built the **foundation** the whole tier depends on, plus the **one money-report fix that's low-risk and unambiguous**. I intentionally **stopped before the report repoints that change live money numbers or need your eyes / new UI** — those are safer to land with you awake and are documented as the next step (rationale below).

### 1. Foundation (additive — changes NO existing behaviour, no money number moves)
- **`Txn.category(t)`** (index.html, in the `Txn` object) — derives the TRUE category (`sale`/`wastage`/`transfer`/`internal`/`delivery`/`return`/`adjustment`/`other_out`) from `type` + `stockTo`/`reason`. `classify()`/`direction` (the `Stock.qty` driver) is **untouched**. A row with no `stockTo` falls to `other_out`, never silently `sale` (forward-only, auditor-confirmed). Helpers: `isSale`/`isConsumption`/`isWastage`.
- **`Stock.stockTypeOf(p)`** + `isRetailProduct`/`isConsumableProduct` — Retail vs Consumable. Explicit `product.stockType` wins; else derived from the existing `internalUse` flag (`true` → consumable) per GPT's migration rule.

### 2. Wastage fix (#9, the clearest audit money-bug — low risk)
- New single definition **`Pages._wastageTxns(d)` = `category==='wastage'`**, used by the wastage **report** (`dirWastage`), the **CSV** (`_exportWastageCSV`), and the **dashboard** stat — so they can't drift. Replaces the old "any OUT carrying a wastage-ish reason" string-match, which wrongly counted a `transfer_out`/sale that happened to carry such a reason.

### Harness
- New sentinels **S-92** (Txn.category per-category labels + stock-math invariance), **S-93** (stockTypeOf derivation), **S-94** (wastage = category only; excludes a transfer carrying a wastage reason).
- **Smoke: 94/94 PASS.** Targeted saboteur S-92/93/94 + full 94-mutation sweep: see the bottom of this file / `audit-artifacts/WAVE-K-FULL-SWEEP.log`.

## Deliberately deferred to your review (plan-approved; each changes live money or needs your call / new UI)
1. **Profit removal → Gross Sales + Total Cost (A on-hand + B period-used).** Removing the broken profit column on the store-comparison screen and replacing it with the two figures — a visible UI + money-number change.
2. **Sell-through (#13)** — exclude consumables + count only `category==='sale'`; per-store denominator = transfers-in, company-wide = deliveries-into-HO. The denominator semantics depend on which view directors actually use — worth confirming live.
3. **Franchise billing retarget (#10)** — bill HO→franchise-**office** wholesale + credit for mistake-returns. Entangled with modelling franchise units as locations (franchise/Azure phase).
4. **Price-at-time snapshot** (GPT change #1) — stamp sale price on sale txns at log time. Touches the **daily log-submit path**; additive but safer to land with you available.
5. **Sync `stockTo`/store-ID metadata** (GPT change #2) — for cross-device category correctness; pairs with the Azure sync work.
6. **CSV (#12)**, **reconciliation baseline (#11)**, **currency capture (#4, needs new form fields)**, **explicit stockType field + product-edit UI**, **"definitions changed" labels**.

**Why stop here:** the foundation + wastage are additive/low-risk and fully sentinel-proven, so they're safe to leave overnight. Everything in the deferred list either changes a number a director reads, adds UI, or needs a small decision from you — exactly the kind of thing the framework says to land with a human checkpoint, not unattended. The auditors have already blessed the *plan* for all of it; this just sequences the *build* sensibly.

## Next steps (your morning)
1. Review this + the built diff.
2. Send the prepared GPT/Gemini code-audit briefs (below / in chat) for the foundation + wastage.
3. Greenlight the deferred repoints (I'll build them with you available), then full sweep → commit Wave K.

---

## Sub-wave K3 — DESIGN (franchise billing #10 + wastage-at-cost + returns) — NOT built, design review first
1. **Wastage valued at cost-at-time:** the 3 wastage sites (dirWastage totalVal ~3581, _exportWastageCSV, dashboard wastageThisMonth ~3638) value wastage at retail `p.price`. Change to `Stock.costAtDate(t.productId,t.date) × qty` (cost basis, per the plan). + sentinel.
2. **Franchise billing (#10) retarget store→office:** current `franchiseInvoice`/`_exportFICSV` bill each franchise STORE for ALL `Txn.isIn` (incl. returns + adjustments + the office→store internal transfer). Per Kunal's confirmed model + GPT: the billable event is **BOB-HO → Franchise OFFICE**. Data: HO→office is recorded as the OFFICE's incoming rows (type:'in' "Received from Head Office", no stockFrom → category 'delivery'; index.html:666). So **bill each franchise OFFICE (`isFranchise && isFranchiseOffice`) for its genuine incoming SUPPLY** = `t.storeId===office.id && Txn.isIn(t) && Txn.category(t) ∈ {delivery, transfer}` in the period — **excludes returns (`return`) + adjustments (`adjustment`)** automatically (category filter). Apply franchise discount (office.franchiseDiscount + product discount). The office→store leg + the store's receipts are NO LONGER billed (internal to the franchise).
3. **Returns:** customer-return netting already done in K2 (`_grossSales`). Franchise mistake-return **credit** + faulty-return **routing** = deferred to the future "Returns & Replacements / Supplier RMA" feature (Kunal). K3 only makes billing EXCLUDE returns/adjustments (so they aren't wrongly billed).

Open design questions for the auditors: (a) is billing the office's incoming delivery+transfer (excl. return/adjustment) the right identification of "HO→franchise supply"? (b) edge: office 'in' rows have no stockFrom → 'delivery'; a hypothetical direct supplier delivery to an office would also bill — acceptable per topology (suppliers→HO only)? (c) one-office-per-franchise linkage OK? (d) wastage-at-cost correct? (e) any landmine.

## Sub-wave K4 — DESIGN (currency + price-at-time + stock-type UI + labels) — review before build
1. **Currency (Option A — "app converts", Kunal):** delivery form gains a Currency chooser (AUD default / Other). If Other: a **currency code** + a single delivery **rate**. On `_saveDelivery`, when Other, multiply EVERY entered cost (per-line unit cost, packaging, labelling, weight stays as-is; header freight/tax/shipping) by the rate → AUD, THEN run the existing landed-cost maths on the AUD values (so cost-history / costAtDate / K2 Total Cost / K3 wastage-at-cost stay correct in AUD). Store `{currency, rate, foreignTotal}` on the delivery as the audit record. Validate rate finite & >0; AUD = no conversion. Open Qs: exact point to apply the rate (before existing UI.money validation, or convert validated AUD?); no double-conversion on edit (`_saveDeliveryPackaging`); display of original-currency on the delivery detail.
2. **Price-at-time sale snapshot:** on `_submitLog`, stamp the current `product.price` onto SALE transactions (field e.g. `unitPriceAtSale`). Update K2 `_grossSales` to prefer the snapshot, else fall back to current price (closes the K2 deferral; history can't be rewritten by a later price edit). Forward-only. Open Qs: which txns (sales only? returns?), field name, hot-path (daily log) safety.
3. **Retail/Consumable explicit field:** product add/edit gains a Retail/Consumable dropdown writing `product.stockType`; migrate `internalUse:true → stockType:'consumable'`. `Stock.stockTypeOf` already prefers explicit `stockType`. Open Qs: write-through vs keep-derive; any code still keyed on `internalUse` that must move.
4. **"Definitions changed" labels:** rename the affected report labels + a small one-time note on wastage / sell-through / store-comparison so directors aren't startled by the corrected numbers.

### K4 design-review brief — GPT (paste)
> BOB Stock App — Wave K4 (Tier 4: currency + price-at-time + stock-type UI + labels) DESIGN review, before build. REVIEW ONLY — report, no edits. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05` (K1/K2/K3 + the harness; K3 awaiting commit). Read the "Sub-wave K4 — DESIGN" section in `WAVE-K-BUILD-STATUS.md`, plus `_saveDelivery`/`_saveDeliveryPackaging` (delivery + landed-cost maths, the currency selector at ~4197), `_submitLog` (~1745), `_grossSales`/`_costUsed`/`Stock.costAtDate`, `Stock.stockTypeOf` + the `internalUse` product flag.
> Plan: (1) currency Option A — enter foreign costs + one rate, convert ALL costs ×rate → AUD before the existing landed-cost maths, store {currency,rate,foreignTotal} on the delivery; (2) snapshot sale price on log so Gross Sales can't be rewritten by later price edits (prefer snapshot, fall back to current); (3) explicit Retail/Consumable stockType field migrated from internalUse; (4) rename report labels + a "definitions changed" note.
> Review for landmines, esp.: currency — where to apply the rate (no double-conversion on a packaging EDIT, no mixing AUD+foreign), validation (rate finite>0), interaction with cost-history/costAtDate (must end up AUD), and the K3 wastage-at-cost / K2 Total Cost which read costAtDate; price-snapshot — hot-path (daily log) safety + which txns + the fallback; stockType migration — any code still keyed on internalUse. Verdict on the plan: sound / sound-with-changes / rework, file:line.

### K4 design-review brief — Gemini (CLI, paste)
> BOB Stock App — Wave K4 (Tier 4) DESIGN review, before build. REVIEW ONLY — read/report, no edits. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`. Read the "Sub-wave K4 — DESIGN" section in `WAVE-K-BUILD-STATUS.md`, plus `_saveDelivery`/`_saveDeliveryPackaging`, `_submitLog`, `_grossSales`/`Stock.costAtDate`, `Stock.stockTypeOf` + `internalUse`.
> Plan: (1) currency "app converts" — foreign costs × one rate → AUD before the existing landed-cost maths, store {currency,rate,foreignTotal}; (2) snapshot sale price at log time so Gross Sales is immune to later price edits; (3) explicit Retail/Consumable stockType migrated from internalUse; (4) report label renames + "definitions changed" note.
> Review for landmines: currency conversion point + no double-conversion on packaging edit + rate validation + cost-history ends up AUD (so K2 Total Cost / K3 wastage-at-cost stay right); price-snapshot hot-path safety + fallback; stockType migration completeness. Verdict: approve / approve-with-changes / block, file:line.

## Sub-wave K3 — BUILT (2026-06-18, design-reviewed by GPT+Gemini, NOT committed — for Kunal's morning review)
Built per the design + both auditors' refinements + Kunal's calls (A: bill only genuine HO supply):
- **Franchise billing #10:** new `Pages._isHOSupply(t)` (HO source: `stockFromStoreId==='head_office'` | base `stockFrom==='HO Warehouse'` | legacy reason `/head office/`) + `Pages._franchiseInvoiceData(d,from,to)` — bills each franchise **OFFICE** for genuine HO supply (`Txn.isIn` && category∈{delivery,transfer} && `_isHOSupply`), excluding returns/adjustments/non-HO; per-product discount over office default; `discMissing` flag (never silent 0%). `franchiseInvoice`/`_exportFICSV` rewired to it; report relabelled (office, "supply charges", Returns note). Seed: `cockburn_office.franchiseDiscount` set 25 (was null).
- **Wastage at cost-at-time:** new `Pages._wastageCostTotal(txns)` (Σ `costAtDate×qty`, counts missing-cost); used by the report + dashboard; per-row + CSV value at cost; labels renamed retail→cost; missing-cost surfaced ("⚠ N w/o cost", "— no cost").
- **Returns:** customer-netting (K2) stands; franchise mistake-return CREDIT + faulty-routing deferred to the Returns/RMA feature; K3 just excludes returns/adjustments from billing.
- **Per-product franchise discount** (incl. 100%=complimentary) preserved.
- **Franchise cost-basis gap** (franchise's cost = what they paid BOB = discounted sell price; app tracks only BOB's landed cost) banked for the franchise-tracking/Azure feature.

Sentinels **S-106** (franchise billing) + **S-107** (wastage-at-cost); mutations S-106 (category filter), S-107 (cost→retail), S-108 (`sentinel:'S-106'`, `_isHOSupply`). Smoke **102/102**; targeted saboteur + full sweep results below.

### K3 code-audit brief — GPT (paste)
> BOB Stock App — Wave K3 (Tier 4: franchise billing #10 + wastage-at-cost) CODE audit. AUDIT ONLY, report only. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05` (uncommitted, on K2 `09fba30`). You design-reviewed this (sound-with-changes); all your refinements are in — verify the build. Read the "Sub-wave K3 — BUILT" section in `WAVE-K-BUILD-STATUS.md`, then in index.html: `Pages._isHOSupply`, `Pages._franchiseInvoiceData`, `franchiseInvoice`/`_exportFICSV`, `Pages._wastageCostTotal`, `dirWastage`/`_exportWastageCSV`/dashboard wastage, and seed `cockburn_office`.
> Verify: (1) franchise billing targets the OFFICE, bills only genuine HO supply (category delivery/transfer && `_isHOSupply`), excludes returns/adjustments/non-HO/shop-receipts; (2) office discount applied, per-product override + 100%-complimentary work, missing-office-discount flagged not silent-0%; (3) `_isHOSupply` correctly identifies HO source incl. the legacy-reason fallback, doesn't over-match; (4) wastage valued at `costAtDate×qty` everywhere (report/dashboard/CSV), missing-cost surfaced not silent-$0, labels are cost-basis; (5) no regression; (6) S-106/107 + mutations S-106/107/108 genuinely catch. Smoke 102/102; full sweep in `audit-artifacts/WAVE-K3-SWEEP.log`. Verdict: PASS / sound-with-changes / BLOCK, file:line.

### K3 code-audit brief — Gemini (CLI, paste)
> BOB Stock App — Wave K3 (Tier 4) CODE audit. AUDIT ONLY — read/report, no edits. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`. You design-reviewed this; verify the build. Read the "Sub-wave K3 — BUILT" section in `WAVE-K-BUILD-STATUS.md`, then `Pages._isHOSupply`/`_franchiseInvoiceData`/`franchiseInvoice`/`_exportFICSV`/`_wastageCostTotal`/`dirWastage`/`_exportWastageCSV`/dashboard wastage + seed `cockburn_office` (index.html).
> Verify: franchise billing → OFFICE, genuine HO supply only (excl. returns/adjustments/non-HO/shop), office discount + per-product override + 100% complimentary + missing-discount flagged; wastage at `costAtDate×qty` everywhere + missing-cost surfaced + cost-basis labels; no regression; S-106/107 + mutations genuinely catch. Smoke 102/102; full sweep `audit-artifacts/WAVE-K3-SWEEP.log`. Verdict: approve / approve-with-changes / block, file:line.

## Sub-wave K2 — Gross Sales + Total Cost + sell-through (built 2026-06-17, Kunal-approved diff; NOT committed)
Built: helpers `_grossSales` (sales − returns), `_costUsed` (cost-at-time of sale+internal+wastage; transfers/adjustments excluded), `_stockOnHandCost` (stock × current cost), `_sellThroughAgg` (extracted: sold=sales-only, received topology-aware, consumables excluded). Store-Comparison "Profit" columns → Gross Sales (A/B/Δ) + Total Cost (A/B/Δ) + On-hand(cost). Sell-through repointed. Sentinels S-97..S-100; smoke 99/99; targeted saboteur 4 CAUGHT/0 BLIND/0 INFRA; full sweep → `audit-artifacts/WAVE-K2-SWEEP.log`. **Gross Sales uses CURRENT price until sub-wave K4 (price-at-time snapshot) — documented deferral.**

## K2 round-1 audit (2026-06-17): GPT BLOCK, Gemini APPROVE → fixed
GPT found real issues Gemini missed (GPT = the gate). Resolution:
- **P1 — company-wide sell-through missed the HO delivery denominator.** Deliveries are booked against `head_office` (a warehouse excluded from `visibleStores`), so the company-wide denominator was empty → null/0 sell-through. **FIXED:** when `companyWide`, the store set now includes HO/warehouse ids so deliveries count. S-99 strengthened (+`cwDeliveryReceived` case) + mutation **S-102**.
- **P1 — Gross Sales return-netting too broad + retail-only not enforced.** **FIXED:** `_grossSales` now (a) counts retail products only, (b) nets ONLY `stockFrom==='Customer'` returns (store/franchise/supplier returns are not refunds). S-97 reworked (customer-return + store-return + consumable cases) + mutations **S-103** (retail-only) / **S-104** (customer-return-only).
- **P2 — manual transfer-in mis-categorised.** The log screen's STOCK-IN from HO/Another Store/Franchise Office was `type:'in'` → all mapped to `delivery`. **FIXED:** `Txn.category` 'in' now maps those sources to `transfer` (supplier/none → delivery). New sentinel **S-101** + mutation.
- **P2 — cross-device category metadata (stockTo/stockFrom not synced): REASONED DEFER to the Azure/sync sub-wave.** Adding those fields to the `_toSharePoint` push payload risks breaking the LIVE push if the Logic App / SharePoint list schema rejects unknown columns — so cross-device category correctness inherently needs the **server-side columns + schema (Azure phase)**, not a client-only change. **K2 is device-local-correct now; cross-device-correct after the Azure sync sub-wave.** Documented bound (Gross Sales also uses current price until K4). This is sequencing, not a gap left unaddressed.
- **P3 — sweep evidence:** the full K2 sweep completed after GPT read it mid-run: **100 CAUGHT / 0 BLIND / 0 SKIPPED / 0 INFRA-FAIL** (baseline 99/99). Re-sweep after these fixes: 100/100 smoke; full sweep pending.

## K2 round-2 audit (2026-06-17): GPT BLOCK (suffix drift), Gemini APPROVE → fixed
GPT cleared both P1s + ACCEPTED the sync-metadata defer (device-local-correct language required), but found the manual-transfer fix incomplete: the UI suffixes a store name onto labels — `"Another Store — Booragoon"`, `"Store Transfer — Booragoon"` (index.html:1738-1739) — so my exact-match `stockFrom === 'Another Store'` missed them (fell to `delivery`), and S-101 was blind (tested only the unsuffixed value). Same drift on the OUT side (not a K2 money leak, but category drift). **FIXED at root:** `Txn.category` now keys off the **base label** — `(t.stockTo||'').split(' — ')[0]` / same for stockFrom — so suffixed labels classify correctly (both in and out). S-101 strengthened to test the real suffixed values (in + out); mutation **S-105** reverts the base-extraction and proves it. Smoke 100/100. **GPT also accepted the sync-metadata sequencing as a bounded deferral (not a blocker).**

### K2 audit brief — GPT (paste)
> BOB Stock App — Wave K2 (Tier 4 money reports) code audit. AUDIT ONLY, report only. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05` (uncommitted, on top of 82b9952). Plan: `audit-artifacts/WAVE-K-TIER4-SCOPE.md` ★ CONSOLIDATED FINAL PLAN + FOLLOW-UP DECISIONS. Built (index.html, in `Pages`): `_grossSales` (Σ sales price×qty MINUS returns), `_costUsed` (Σ costAtDate×qty over category sale/internal/wastage only), `_stockOnHandCost` (Σ Stock.qty×currentCost per store), `_sellThroughAgg` (per-product {in,out}: out=category 'sale' only, in=companyWide?delivery:transfer-in, retail products only). `_renderComparisonData` director columns now Gross Sales + Total Cost + On-hand (was a profit calc that counted every outflow). `sellThrough()` uses `_sellThroughAgg` + filters retail. Sentinels S-97..S-100; smoke 99/99; full sweep in `audit-artifacts/WAVE-K2-SWEEP.log`.
> Verify: (1) Gross Sales = sales−returns, no other category leaks; returns-netting sound; (2) Total Cost categories correct (sale+internal+wastage; transfers/adjustments excluded) and cost-at-time (costAtDate) used; (3) sell-through: sold=sales-only, the companyWide (`isHO && !storeFilter`) vs per-store "received" switch is correct and doesn't double-count, consumables excluded; (4) on-hand = current stock×cost, point-in-time labelled clearly; (5) NON-director / non-HO paths and the existing Units/Value columns unaffected (no regression); (6) the current-price deferral (until K4) is acceptable + clearly bounded; (7) S-97..S-100 genuinely catch. Verdict: PASS / sound-with-changes / BLOCK, file:line.

### K2 audit brief — Gemini (CLI, paste)
> BOB Stock App — Wave K2 (Tier 4 money reports) code audit. AUDIT ONLY — read/report, no edits. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`. Plan: `audit-artifacts/WAVE-K-TIER4-SCOPE.md` (★ CONSOLIDATED FINAL PLAN + FOLLOW-UP DECISIONS). Built (index.html, `Pages`): `_grossSales` (sales−returns), `_costUsed` (cost-at-time of sale/internal/wastage; transfers+adjustments excluded), `_stockOnHandCost` (stock×currentCost), `_sellThroughAgg` (sold=sales-only; received=companyWide?delivery:transfer-in; consumables excluded). Store-Comparison profit columns → Gross Sales + Total Cost + On-hand; sell-through repointed. Sentinels S-97..S-100; smoke 99/99; full sweep `audit-artifacts/WAVE-K2-SWEEP.log`.
> Verify the same 7 points: gross-sales = sales−returns (no leak); cost categories + cost-at-time; sell-through sold/received topology + consumable exclusion + no double-count; on-hand point-in-time; no regression to non-director/Units/Value; current-price deferral acceptable; S-97..100 genuinely catch. Verdict: approve / approve-with-changes / block, file:line.

## Prepared audit briefs (foundation + wastage code review) — matched, report-only

### Paste to GPT
> BOB Stock App — Wave K Tier 4 PARTIAL build (foundation + wastage). AUDIT ONLY — do not edit any files; written report. This reviews the BUILT code; the rest of Tier 4 is plan-approved and intentionally deferred (see WAVE-K-BUILD-STATUS.md).
> Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05` (Wave J committed 30d6d6e; this build uncommitted). Read `WAVE-K-BUILD-STATUS.md` and the locked plan `audit-artifacts/WAVE-K-TIER4-SCOPE.md` (★ CONSOLIDATED FINAL PLAN). Built: `Txn.category(t)` + `isSale/isConsumption/isWastage` (index.html, in `Txn`), `Stock.stockTypeOf`/`isRetailProduct`/`isConsumableProduct` (index.html, in `Stock`), `Pages._wastageTxns(d)` + its use in `dirWastage`/`_exportWastageCSV`/dashboard. Sentinels S-92/93/94; smoke 94/94; full 94-mutation sweep result in `audit-artifacts/WAVE-K-FULL-SWEEP.log`.
> Verify: (1) `Txn.category` is correct for every type+stockTo, and a no-`stockTo` 'out' falls to `other_out` (never silently `sale`); (2) it is genuinely reporting-only — `classify()`/`direction`/`Stock.qty` are untouched (stock-math invariance); (3) `stockTypeOf` migration (`internalUse`→consumable, explicit wins) is sound and won't false-classify; (4) `_wastageTxns` correctly counts category==='wastage' and EXCLUDES a transfer_out/sale carrying a stray wastage reason, and all three call sites are consistently repointed; (5) any regression in the touched reports; (6) are S-92/93/94 genuinely catching (not blind/over-determined)? Verdict: PASS / sound-with-changes / BLOCK, file:line.

### Paste to Gemini (CLI)
> BOB Stock App — Wave K Tier 4 PARTIAL build (foundation + wastage). AUDIT ONLY — read and report; do NOT edit, write, or modify any files; written report only. Reviews the BUILT code; the rest of Tier 4 is plan-approved and intentionally deferred (see WAVE-K-BUILD-STATUS.md).
> Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05` (Wave J committed 30d6d6e; this build uncommitted). Read `WAVE-K-BUILD-STATUS.md` and the locked plan `audit-artifacts/WAVE-K-TIER4-SCOPE.md` (★ CONSOLIDATED FINAL PLAN). Built: `Txn.category(t)` + `isSale/isConsumption/isWastage`, `Stock.stockTypeOf`/`isRetailProduct`/`isConsumableProduct`, `Pages._wastageTxns(d)` + its use in `dirWastage`/`_exportWastageCSV`/dashboard. Sentinels S-92/93/94; smoke 94/94; full sweep in `audit-artifacts/WAVE-K-FULL-SWEEP.log`.
> Verify: (1) `Txn.category` correct for every type+stockTo, no-`stockTo` 'out' -> `other_out` never silently `sale`; (2) reporting-only — `classify()`/`direction`/`Stock.qty` untouched (stock-math invariance); (3) `stockTypeOf` migration sound; (4) `_wastageTxns` counts category==='wastage' only and excludes a transfer/sale carrying a wastage reason, all three sites repointed; (5) any regression; (6) S-92/93/94 genuinely catching. Verdict: approve / approve-with-changes / block, file:line.

## Round-1 audit + fixes (2026-06-17)
- **GPT: BLOCK** (2 real bugs + a framing gap + sentinel-strength notes). **Gemini: APPROVE** — but Gemini *praised* the very `isWasteReason` logic GPT flagged as a bug. **Adjudicated in GPT's favour** (it matches the locked plan: destination/`stockTo` is authoritative). Classic two-auditor value.
- **Fixed:** (1) `Txn.category` — `stockTo` is now authoritative; a `reason:'Wastage'` no longer overrides a transfer/in-house/sale destination, and a no-`stockTo` out stays `other_out` (dropped the `isWasteReason` override). (2) `Stock.stockTypeOf` — an invalid explicit `stockType` now returns `'unknown'` (surfaced, never silently retail).
- **Sentinels strengthened/added:** S-92 (+reason-override edge cases), S-94 (+log-screen `out`+Store-Transfer encoding), **S-95** (invalid stockType → unknown). Smoke **95/95**; targeted saboteur **S-92/93/94/95 = 4 CAUGHT / 0 BLIND**.
- **Wastage valuation = EXPLICITLY DEFERRED** (corrects the earlier framing). The report/CSV/dashboard still value wastage at current retail `p.price`; the plan calls for **cost-at-time** (`costAtDate`). That's a live money-meaning change → deferred to the Kunal-present batch with the other report repoints. Both auditors agreed deferral is fine.

## Re-audit brief (GPT — clear the BLOCK) + Gemini
### Paste to GPT
> BOB Stock App — Wave K Tier 4 partial re-audit (your round-1 BLOCK). AUDIT ONLY, report only. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`. Your 2 findings are fixed: (1) `Txn.category` (index.html, in `Txn`) now makes `stockTo` authoritative — a `reason:'Wastage'` no longer overrides a transfer/in-house/sale destination, no-`stockTo` out → `other_out`; (2) `Stock.stockTypeOf` returns `'unknown'` for an invalid explicit `stockType` (never silently retail). Sentinels strengthened: S-92 (reason-override cases), S-94 (out+Store-Transfer wastage-reason), S-95 (invalid stockType). Wastage cost-at-time valuation is EXPLICITLY DEFERRED (see WAVE-K-BUILD-STATUS.md). Smoke 95/95; S-92/93/94/95 mutation-proven. Verify both fixes are complete and the sentinels genuinely catch; confirm BLOCK cleared. Verdict: PASS / sound-with-changes / BLOCK, file:line.

### Paste to Gemini (CLI)
> BOB Stock App — Wave K Tier 4 partial re-audit. AUDIT ONLY — read/report, no edits. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`. Since your APPROVE, GPT found (and I fixed) two things: `Txn.category` now treats `stockTo` (destination) as authoritative — a stray `reason:'Wastage'` no longer reclassifies a transfer/in-house movement as wastage (this matches the locked plan; the earlier `isWasteReason` override you praised was actually a plan-violation); and `Stock.stockTypeOf` surfaces an invalid explicit stockType as `'unknown'`. Sentinels S-92/94 strengthened, S-95 added; smoke 95/95. Please re-verify these two changes are correct and don't regress, and confirm you concur that destination-authoritative is right. Verdict: approve / approve-with-changes / block, file:line.

## Round-1 RE-AUDIT + harness hardening (2026-06-17)
**Part A (code fixes): BOTH PASS** — GPT explicitly cleared its round-1 BLOCK; Gemini APPROVE. Foundation + wastage code is blessed.

**Part B (harness parallelism): Gemini APPROVE, GPT BLOCK-as-commit-gate** — 2 real issues, both fixed:
- **P2 collision:** `bob-sab-<id>` temp dirs were unique only within one process → overlapping/leftover saboteur runs could clobber each other (GPT found live dirs while auditing). **Fixed:** a run-unique root via `fs.mkdtempSync('bob-sab-run-')`, each mutation under `<root>/<id>`.
- **P2 infra-failure:** partial/crashed/timed-out child runs could pass as a clean baseline or be miscounted as CAUGHT/BLIND. **Fixed:** `parseSmoke` now detects the smoke's completion summary line; baseline requires a COMPLETE run (summary present, `total === EXPECTED_SENTINELS`, all green); a mutation run that didn't complete (no summary) or whose target sentinel never ran is **`[INFRA-FAIL]`** (not CAUGHT/BLIND) and fails the gate. Output now reports `… N INFRA-FAIL …`.
- **GPT note (adopted):** added a dedicated reason-override mutation **S-96** (`sentinel: 'S-92'`) that *reintroduces* the `reason`-promotes-to-wastage bug, proving S-92 catches the destination-authoritative regression head-on (not just the missing-wastage-branch case). The runner gained `sentinel:` support (a mutation may target another mutation's sentinel; default = its own id).

**Validated:** hardened runner on S-92/S-95/S-96 = 3 CAUGHT / 0 BLIND / 0 INFRA-FAIL; S-96 correctly flipped **S-92**. Full hardened sweep (96 mutations, concurrency 6) running → `audit-artifacts/WAVE-K-SWEEP-hardened.log`.

## Round-2 harness re-audit (GPT BLOCK #2 → resolved 2026-06-17)
GPT BLOCK'd again: the collision fix was confirmed, but the infra-completeness fix was incomplete — a mutation could flip its target then the suite could abort before all 95 sentinels ran, and still count. GPT required `total === EXPECTED_SENTINELS` for every result. **But a diagnostic showed that's too strict:** S-37 (and other broad mutations) are *designed* to flip their sentinel EARLY and then abort the rest of the suite (the mutation breaks clean boot), so a full run is impossible by design — yet S-37 is a legitimate CAUGHT (verified in the last Wave-J sweep). A blanket "require all 95" wrongly flags S-37 as INFRA-FAIL (diagnostic: `[INFRA-FAIL] S-37 :: 1/95`).
**Correct fix (implemented):** trust a COMPLETE run outright; trust a PARTIAL run only if the target's verdict is **deterministic — identical across the original + a retry** (a real broad mutation flips the same way every time; flaky contention varies → INFRA-FAIL). Validated: S-37 → CAUGHT (deterministic-partial path), S-92 → CAUGHT (complete), 0 INFRA-FAIL.

**GPT PASS (2026-06-17):** accepts the determinism-across-retry rule; both Part-B P2s cleared, S-96 mapping correct, exit gate fails on any infra. **HARNESS CERTIFIED — both auditors PASS Part A (code) AND Part B (harness).** Only the completed full-sweep artifact remained (sweep was mid-run when GPT read it).

### Harness re-audit brief v2 (GPT — clear Part-B BLOCK)
> BOB Stock App — saboteur-runner.js harness re-audit v2. AUDIT ONLY, report only. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`. Your BLOCK#2 (partial runs still admissible) is addressed, but NOT by your literal "require total === EXPECTED_SENTINELS" — that rule is too strict for this harness: some mutations (e.g. **S-37**) are *intentionally caught by an EARLY sentinel that flips RED and THEN aborts the rest of the suite* (the mutation breaks clean boot, so a full 95-sentinel run is impossible by design; S-37 is a legitimate CAUGHT — see the last Wave-J sweep). A blanket full-count requirement wrongly flags S-37 INFRA-FAIL (diagnostic confirmed: `1/95`). **Implemented instead (test/saboteur-runner.js, processMutation):** a COMPLETE run (`summary && total === EXPECTED_SENTINELS`) is trusted outright; a PARTIAL run is trusted ONLY if the target sentinel's verdict is **deterministic — identical across the original run AND a retry** (real broad mutation = same verdict every time; flaky/contention = varies → `[INFRA-FAIL]`). Target absent, or verdicts disagree → INFRA-FAIL (fails the gate). Validated: S-37 → CAUGHT via the deterministic-partial path, S-92 → CAUGHT via a complete run, 0 INFRA-FAIL; full sweep in `audit-artifacts/WAVE-K-SWEEP-hardened.log`. Do you accept the determinism-across-retry rule as a sound substitute for blanket full-count (it rejects flaky partials but admits deterministic broad-mutation partials)? Verdict: PASS / sound-with-changes / BLOCK, file:line.

### (superseded) Harness re-audit brief v1 (GPT — clear Part-B BLOCK)
> BOB Stock App — saboteur-runner.js harness re-audit (your Part-B BLOCK). AUDIT ONLY, report only. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`. Both your harness P2s are fixed in `test/saboteur-runner.js`: (1) run-unique temp root via `fs.mkdtempSync('bob-sab-run-')` + per-mutation `<root>/<id>` (collision-free across overlapping runs); (2) `parseSmoke` detects the smoke completion-summary line — baseline now requires a complete run (summary present, total === EXPECTED_SENTINELS, all green), and a mutation run with no summary / missing target sentinel is classified `[INFRA-FAIL]` (not CAUGHT/BLIND) and fails the gate. Also added mutation S-96 (`sentinel:'S-92'`) reintroducing the reason-override for stronger proof; runner now supports a mutation targeting another sentinel via `sentinel:`. Validated: hardened run = 3 CAUGHT/0 BLIND/0 INFRA-FAIL; full sweep in `audit-artifacts/WAVE-K-SWEEP-hardened.log`. Confirm the two P2s are resolved and the runner is now safe as a commit gate. Verdict: PASS / sound-with-changes / BLOCK, file:line.

*(Full hardened-sweep result will be appended when it lands.)*
