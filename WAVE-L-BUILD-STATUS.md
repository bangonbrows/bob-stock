# Wave L (Tier 5 — Transfer subsystem) — BUILD STATUS

Branch `fix/cli-tri-audit-2026-05`. Tier 5 = the transfer subsystem cluster (phase2.js), the 2nd-biggest weak area in the 6-way blind audit. Sole engineer: Claude. GPT + Gemini audit (run-the-code, report-only). Commit only on Kunal's explicit OK after both auditors PASS. Harness baseline at Tier-5 start: 109 sentinels / 116 mutations (after K4 `ebbfa79`).

## Discovery triage (2026-06-18, verified against CURRENT code — not the audit snapshot)
Several audit-doc findings were already remediated in Waves A–K. Verified per-finding:

| # | Finding | Verdict | Where | Note |
|---|---------|---------|-------|------|
| 1 | GPT-15 completeFlags partial resolution | **FIXED** | phase2.js:1583/306-324 | now one atomic `resolveAllFlags` → single durable write |
| 2 | GPT-12 confirmDraftItem fire-and-forget | **LIVE** | phase2.js:122-130 | non-durable `updateTransfer`+`commit`, no await/confirm |
| 3 | GPT-14 invalid qty coerced to 0 | **PARTIAL** | phase2.js:80,148 | domain coerces blank→0-unit line; only UI rejects |
| 4 | GPTa-22 create can drive origin negative | **LIVE** | phase2.js:62-106/132-170 | no `Stock.qty(pid,fromStoreId)` source check in domain |
| 5 | GPTa-23 receive stores raw receivedQty | **PARTIAL** | phase2.js:211-212 | over-receipt rejection fixed (191-201); stored value still raw |
| 6 | GPTa-26 franchisee multi-store visibility | **PARTIAL** | phase2.js:350 | `Transfer.list` uses single-store `Auth.storeId()`; hub UI already uses `storeIds()` |
| 7 | GPTa-27 return transfers lose type | **LIVE** | phase2.js:72-86 | never sets `type:'return'`; hub always renders "Standard" |
| 8 | GPTa-29 atomic writes don't schedule sync | **LIVE** | db.js:816/853 | `atomicTransferWriteDurable`/`atomicDeliveryWrite` omit `scheduleSync` → stranded stock |
| 9 | GPTa-21 stale cancel double-counts | **LIVE** | phase2.js:360-381 | cancel reverses stock with only local status check; no transfer_in-exists guard |
| 10 | Ca-role _canViewHistory isAtLeast bug | **LIVE** | phase2.js:48 | `isAtLeast('store_manager')` denies franchisee (ranks below); file warns about this exact trap at index.html:918 |
| 11 | Ca-M10 negative stock never floored/flagged | **LIVE** | index.html:1071/1086 | computed sum can go negative; no display floor, no alert |
| 12 | W8 logout doesn't clear transfer draft | **PARTIAL** | index.html:5187 vs phase2.js:1642 | logout calls `TransferUI.resetState` but resetState lives on `Transfer` → undefined, short-circuits; transfer `_txState` leaks |
| 13 | G1-15 multi-table sync gap | **LIVE (by design)** | sync.js | transfers/deliveries/stockTakes records still don't sync — **AZURE-DEFERRED** (needs Logic App endpoints + SharePoint lists) |

## Client-fixable now (this tier) — 11 items
#2, #3, #4, #5, #6, #7, #8, #9 (interim client guard only), #10, #11, #12.

## Inherently server/Azure (deferred, banked)
- #9 full cross-device cancel idempotency (client gets an interim transfer_in-exists guard now; authoritative = server `(TransferId,Type)` idempotency).
- #13 multi-table sync of transfer/delivery/stocktake records.

## Kunal decisions (locked 2026-06-18)
- **Over-send (#4): HARD-BLOCK.** The domain `create`/`submitDraft` must refuse a line whose qty exceeds `Stock.qty(pid, fromStoreId)` (transfers are infrequent; screen already caps it; this closes the back-door for stale/console/regression). Error tells the user to sync / stock-take.
- **Negative stock (#11): show 0 to staff + flag to director.** Floor the DISPLAYED qty at 0 everywhere staff see it (never a negative number); surface a ⚠ negative-stock alert to the director only; do NOT rewrite the underlying ledger value.
- **NEW — manual Sync + visibility (approved):** add a "Sync now" button + a "Last synced … / Syncing…" status indicator to BOTH the sidebar nav and the mobile top bar; also AUTO-SYNC on reconnect (the `online` event currently only hides the offline bar — make it trigger a sync/push+poll). Honest caveat recorded: this refreshes what already syncs (stock levels, catalogue); it will NOT surface transfer/delivery/stocktake RECORDS cross-device until the Azure multi-table sync (#13).

## Current sync behaviour (verified 2026-06-18)
Auto-PUSH on every local write (DB.commit → scheduleSync → 800ms debounce → push). Auto-PULL poll every 30s (`POLL_INTERVAL:30000`). Multi-tab leader election (only leader syncs). Offline bar on disconnect. Sync-status toast bottom-left (transient). "Last sync" shown only in Settings → Cloud Sync. GAPS: reconnect does NOT trigger sync; no persistent nav sync control/status; transfer/delivery durable writes don't scheduleSync (#8).

## Proposed sub-waves
- **L1 — Transfer domain integrity** (ledger/stock correctness): #3 reject invalid qty, #4 source-stock HARD-BLOCK, #5 validate stored receivedQty, #7 return type, #9 cancel transfer_in-exists guard.
- **L2 — Sync reliability + visibility**: #2 confirmDraftItem durable, #8 `scheduleSync` on all durable ledger writes (shared `_afterLedgerWrite` hook), + auto-sync-on-reconnect + "Sync now" button + "last synced/syncing" status (sidebar nav + mobile top bar).
- **L3 — Access / visibility / UX safety**: #6 `Transfer.list` → `storeIds`, #10 `_canViewHistory` explicit role set, #11 negative-stock display floor + director alert, #12 logout transfer-draft reset.

Each sub-wave: discover → build → sentinels → saboteur (targeted + full sweep 0-blind) → GPT+Gemini audit → commit on Kunal OK.

## WAVE L1 BUILT (2026-06-18, NOT committed)
phase2.js — Transfer domain integrity:
- #3 (GPT-14): `create` validates each line qty via `Validate.qty` and rejects ≤0/blank/fractional (was `Math.max(0,safeInt||0)` → 0-unit lines); `submitDraft` re-validates edited qtys (restores snapshot on reject).
- #4 (GPTa-22, HARD-BLOCK): `create` (non-draft) + `submitDraft` refuse any line qty > `Stock.qty(pid, fromStoreId)` — origin can't be driven negative; draft restored on reject. Client guard only (P-13); server idempotency = the authoritative fix.
- #5 (GPTa-23): `receive` stores `Math.max(0, Math.trunc(Number(receivedQty))||0)` (was raw — string/fraction/negative persisted).
- #7 (GPTa-27): transfer object now carries `type: opt.type || (returnReason ? 'return' : 'standard')` so the hub stops rendering returns as Standard.
- #9 (GPTa-21, interim client guard): `cancel` refuses if any `transfer_in` ledger row exists for the transfer (another device received it) — prevents phantom-stock reversal. Full cross-device fix = server `(TransferId,Type)` idempotency (Azure).
Sentinels S-117 (qty reject), S-118 (over-send block, create+submitDraft), S-119 (receivedQty validated), S-120 (return type), S-121 (cancel guard). Mutations S-117/118/119/120/121 + S-122 (sentinel:S-118, submitDraft branch). Smoke 114/114; targeted 6 CAUGHT/0 BLIND/0 INFRA. Full sweep → audit-artifacts/WAVE-L1-SWEEP.log.

## WAVE L1 — internal gate MET (2026-06-18)
Smoke 114/114. Full sweep = baseline 114/114, **122 CAUGHT / 0 BLIND / 0 SKIPPED / 0 INFRA-FAIL** (confirmed stable across 2 consecutive clean-capture runs). Harness now 114 sentinels / 122 mutations.
Harness incidents (banked, P-12 change-safety): my new create/submitDraft over-send + qty guards MASKED two existing transfer sentinels — **S-03** (submitDraft email-on-failed-write: the test store had no stock → guard rejected before the notify/write path; fixed by seeding stock + driving `Transfer.submitDraft(id,{pid:2})` directly instead of the DOM-reading `TransferUI.submitDraft`) and **S-15** (create actor-slim: no stock → over-send guard blocked the transfer → no transfer_out row → actor-leak mutation couldn't manifest; fixed by seeding origin stock). One additional transient false-blind appeared once under peak parallel load with NO printed verdict line (a complete-but-flaky-green pass; the determinism retry only covers PARTIAL runs) — did not reproduce across two subsequent full sweeps. LESSON: adding a domain precondition guard can silently mask any existing sentinel whose setup didn't satisfy the new precondition — the FULL sweep catches it (targeted runs don't); seed the precondition in those sentinels.

### L1 code-audit brief — GPT (paste as-is)
> BOB Stock App — Wave L1 (Tier 5: transfer domain integrity) CODE audit. AUDIT ONLY — report findings, do NOT edit. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`, file `phase2.js` (Transfer module). RUN the code (browser/Node vm), don't review statically. Five domain fixes to verify, adversarially:
> 1. **Invalid qty (GPT-14)** — `create` + `submitDraft` must reject a line whose qty isn't a positive whole number (was `Math.max(0,safeInt||0)` → 0-unit lines + 0-qty ledger rows). Check via `Validate.qty`; confirm no 0/blank/fractional/negative line can create a transfer or a transfer_out row. `submitDraft` must restore the pre-mutation snapshot on reject (it mutates `t` in place).
> 2. **Over-send HARD-BLOCK (GPTa-22, Kunal decision)** — `create` (non-draft) + `submitDraft` must refuse any line qty > `Stock.qty(pid, fromStoreId)`; origin stock can't be driven negative; draft restored on reject. A DRAFT create must NOT apply the source check (no ledger rows yet). Confirm both branches enforce it and there's no path that writes transfer_out without the check.
> 3. **receivedQty validation (GPTa-23)** — `receive` must store a validated whole non-negative `item.receivedQty` (was raw — string/fraction/negative persisted); confirm the stored value and the credit are consistent, and over-receipt is still rejected.
> 4. **Return type (GPTa-27)** — the transfer object carries `type` = 'return' when `returnReason` set, else 'standard'; confirm the hub renders it and nothing else depends on the missing field.
> 5. **Cancel guard (GPTa-21, interim)** — `cancel` must refuse if any `transfer_in` ledger row exists for the transfer (another device received it), preventing phantom-stock reversal. Confirm it doesn't false-block a legitimate cancel of a never-received in_transit transfer, and that the guard is correctly scoped to in_transit. Note this is an interim client guard; the authoritative fix is server idempotency (Azure) — confirm that's the right boundary.
> Also: do these guards introduce any regression in the create/submitDraft/receive/cancel happy paths? Report P0–P3 with file:line. Verdict: PASS / sound-with-changes / BLOCK.

### L1 code-audit brief — Gemini (paste as-is; CLI, report-only)
> BOB Stock App — Wave L1 (Tier 5: transfer domain integrity) CODE audit. AUDIT ONLY — report findings, do NOT edit any file (reviewer, not engineer). Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`, file `phase2.js`. RUN the code (browser/Node vm), don't review statically. Verify these five Transfer domain fixes and check for regressions in the happy paths:
> 1. `create`/`submitDraft` reject non-positive/blank/fractional qty (no 0-unit lines or 0-qty ledger rows); submitDraft restores its snapshot on reject.
> 2. `create` (non-draft) + `submitDraft` HARD-BLOCK sending more than `Stock.qty(pid, fromStoreId)` (origin never negative; draft restored); a DRAFT create does not apply the source check.
> 3. `receive` stores a validated whole `receivedQty` (not raw string/fraction/negative); over-receipt still rejected.
> 4. transfer `type` = return|standard so the hub stops showing every transfer as "Standard".
> 5. `cancel` refuses when a `transfer_in` already exists (another device received) and does not false-block a legitimate cancel.
> Report issues with file:line and severity. Verdict: PASS / approve-with-changes / BLOCK.

## WAVE L1 CODE AUDIT R1 (2026-06-18): GPT sound-with-changes (1 P2) — FIXED; Gemini PASS (missed it)
**GPT P2 (real):** the receivedQty fix used `Math.trunc(Number(...))||0` which COERCED instead of validating — `'2.9'`→2 silently completed a transfer on garbage input; `-1`→0; contradicts Validate.qty's no-truncation contract. **FIXED:** `receive` now runs a Validate.qty pre-pass over every received line BEFORE any mutation — rejects fractions/negative/garbage (whole non-negative incl 0 allowed), then rejects over-receipt, building a validated `_rq` map the credit loop uses. S-119 reworked: a fractional receivedQty is now REJECTED (nothing stored, transfer untouched) + a valid whole accepted; mutation reverts to truncation → CAUGHT. S-69 (over-receive) still passes (12>10 rejected as a valid whole).
**Gemini R1:** PASS — but explicitly "VERIFIED" the truncation line (phase2.js:250 `Math.trunc`) as correct, i.e. waved through the exact P2 GPT caught (recurring: GPT is the edge-case gate). All five fixes otherwise independently confirmed by Gemini incl. a Create→Receive happy-path regression script.
Smoke 114/114; targeted S-119 CAUGHT. Full sweep after the P2 fix → audit-artifacts/WAVE-L1-SWEEP.log.

### L1 re-audit brief — GPT (paste as-is; confirm the receivedQty P2 fix)
> BOB Stock App — Wave L1 RE-AUDIT (your receivedQty P2 fix). AUDIT ONLY, report only. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`, file `phase2.js`. RUN the code. Your P2 (Transfer.receive truncated received qty via Math.trunc instead of validating — '2.9'→2 silently completed a transfer, -1→0) is fixed: `receive` now runs a `Validate.qty` pre-pass over EVERY received line BEFORE any mutation, rejecting fractions/negative/garbage (whole non-negative incl 0 allowed) and rejecting over-receipt, then uses a validated qty map for crediting. Re-confirm: (1) a fractional/negative/garbage receivedQty is rejected with nothing stored and the transfer untouched (still in_transit); (2) a valid whole receive (incl exact-match and short receipt) still works and credits correctly; (3) over-receipt still rejected; (4) no path stores/credits an unvalidated qty; (5) no regression in the receive happy path or flag/credit logic. Verdict: PASS / sound-with-changes / BLOCK, file:line.

## WAVE L2 SCOPE (discovery done 2026-06-18) — sync reliability + manual Sync
Targets (file:line from L2 discovery):
- **#2 confirmDraftItem durable** — phase2.js:143-151 is non-durable (`DB.updateTransfer(t); DB.commit()`, sync, returns ok unconditionally). Fix: make async, snapshot t, `await DB.updateTransferDurable(t, snap)` (db.js:770, returns Promise<bool>, restores snapshot on fail), gate success. No scheduleSync needed (status-only edit, no ledger rows).
- **#8 scheduleSync on durable writes** — db.js `addTransactionDurable` (~707/720), `atomicTransferWriteDurable` (~816/850), `atomicDeliveryWrite` (~853/883) none call scheduleSync. Fix: shared `DB._afterLedgerWrite()` = guarded `Sync.scheduleSync()`, call `if(ok) this._afterLedgerWrite();` before each `return ok` (matches commitDurable success-gated pattern at db.js:754). Also addTransactionsDurable (~738) if present.
- **NEW Sync.syncNow()** — sync.js has push(663, local→server), pull(880, server→local), poll(1172, leader: pull+push), scheduleSync(1139, LEADER-GATED: a follower only postMessages the leader + 800ms debounce — WRONG for a manual button). Add `async syncNow(){ await push(); await pull(); }` ignoring _isLeader (relies on _syncLock for mutual exclusion); progress shows via existing _showStatus toast (#sync-status).
- **NEW auto-sync-on-reconnect** — index.html:5470-5477 `online` handler only toggles #offline-bar. Add `Sync.syncNow()` on the ONLINE event only (not the shared updateOnlineStatus initial call).
- **NEW Sync-now button + "last synced" status** — desktop sidebar is JS-built by buildSidebar() (index.html:4896-5044; static #sidebar 321-335 is overwritten); add a `sidebar-footer-btn` "Sync now" + a persistent "Last synced …" line in the footer (5036-5039). Mobile top bar is static (index.html:337-341); add a `btn btn-sm` Sync button next to #mobile-logout. "Last synced" reads `bob_last_sync` (epoch ms, written by pull at sync.js:1084; format via `new Date(x).toLocaleString()` / 'Never', per index.html:3411-3412). Keep transient #sync-status (283) for progress only; add a SEPARATE persistent indicator.
NOTE: bob_last_sync advances on PULL only — consider stamping it on push success too so "last synced" reflects both directions (decide at build). Honest caveat (already told Kunal): syncNow refreshes stock/catalogue; transfer/delivery/stocktake RECORDS still don't sync till Azure (#13).
Sub-wave order within L2: (a) #8 _afterLedgerWrite + #2 durable (phase2/db, pure reliability), (b) Sync.syncNow + reconnect (sync.js + online handler), (c) nav UI (button + status, both bars). Sentinels: confirmDraftItem-durable, atomic-writes-schedule-sync (transfer + delivery), syncNow-pushes-and-pulls, reconnect-triggers-sync. UI button = lighter sentinel (presence/onclick wiring).

## WAVE L2 DESIGN REVIEW (2026-06-18): GPT REWORK + Gemini REWORK — STRONGLY CONVERGED. Plan revised.
Both auditors independently flagged the same 3 substantive design flaws (no conflicts). Adopted:
- **(P0, both) bob_last_sync is the PULL CURSOR, not a display field** — verified: loaded into `_lastSyncAt` at sync.js:83/151; advanced only by pull (1084/946). Stamping it on push with the device clock would skip server rows newer than the local clock = silent data loss. → NEVER touch bob_last_sync for UI. Add a SEPARATE display-only key `bob_ui_last_sync` for the nav indicator (stamp on any sync activity). bob_last_sync stays pull-only.
- **(P1, both) syncNow ignoring _isLeader breaks the single-syncer invariant** — `_syncLock` is per-tab/in-memory, NOT cross-tab; a follower running push/pull while the leader polls → duplicate pushes, concurrent pulls, cursor/status regression. → Manual button + reconnect call a LEADER-OWNED QUEUED cycle: if this tab is leader, run push→pull under _syncLock (queue ONE follow-up if already syncing); if follower, BroadcastChannel-message the leader to run it; if NO leader responds within a short timeout, claim leadership via the existing election then run. Reuse/extend the existing 'local-write' BroadcastChannel handler.
- **(P1, both) confirmDraftItem async-in-loop race** — verified called inside a loop at phase2.js:1532-1534 (submit path). Making it async naively → concurrent unawaited durable writes on the same transfer row, racing submitDraft's atomic write. → interactive single-toggle becomes async+durable (await updateTransferDurable); the SUBMIT path stops per-item confirmDraftItem and instead marks items confirmed in memory + lets submitDraft do its ONE atomic durable write (or for...of await). 
- **(P2) _afterLedgerWrite** must run only AFTER the Dexie transaction completes, only on ok===true, wrapped in try/catch (a sync-schedule failure must not make a successful local write look failed). Apply to atomicTransferWriteDurable + atomicDeliveryWrite + bulk addTransactionsDurable. NOTE single addTransactionDurable is already followed by DB.commit() (schedules) in the movement path (index.html:1795) → hooking it double-schedules (harmless via 800ms debounce, but noisy); decide at build (lean: hook the stranded writers only).
- **(P2) reconnect** debounced (2–3s), routed through the same queued leader path, NOT fired from the initial updateOnlineStatus() call.
- **(P3) order + wording** — push-then-pull for the button ("send my work, then catch up"); UI label "Stock last synced" (NOT generic "Last synced") so it doesn't imply transfer/delivery/stocktake RECORDS sync cross-device (still Azure #13).
REVISED L2 = bigger/more careful than the naive plan. Build to this shape, then code-audit. Design-review briefs + verdicts archived above.

## WAVE L2 BUILT (2026-06-19, NOT committed) — built to the design-reviewed shape
- **#8 stranded-stock (db.js):** new `DB._afterLedgerWrite()` (guarded `Sync.scheduleSync()`, try/catch so a sync-schedule error can't fail a successful write), called `if(ok)` after the durable write in `atomicTransferWriteDurable`, `atomicDeliveryWrite`, and bulk `addTransactionsDurable`. Transfer/delivery writes now trigger a push.
- **#2 confirmDraftItem (phase2.js):** now `async` + durable (snapshot → toggle → `await DB.updateTransferDurable(t, snap)` → fatal-error + ok:false on failure). Its only caller, the submit-path loop (TransferUI.submitDraft), no longer calls it per-item — it marks confirmed items in memory and lets `Transfer.submitDraft`'s single atomic durable write persist (avoids the N-concurrent-writes race both auditors flagged).
- **#3 syncNow (sync.js):** new `Sync.syncNow()` — leader runs `_runSyncCycle()` (push→pull, queues ONE follow-up if mid-cycle); follower posts `request-sync` over BroadcastChannel (new handler case: leader refreshes then runs the cycle); if no live leader, the tab claims leadership then runs. Never push/pull directly from a follower.
- **#4 reconnect (index.html):** the `online` event now fires `Sync.syncNow()` DEBOUNCED 2.5s (not from the initial updateOnlineStatus call).
- **#5 display timestamp (sync.js + index.html):** new `Sync._stampUiSync()` writes display-only `bob_ui_last_sync` (NEVER `bob_last_sync` the pull cursor), stamped at all 3 push/pull success points. Nav shows "Stock last synced: …" (label wording per GPT P3 — only stock/catalogue sync cross-device, not transfer records until Azure). Updated on render, after sync, via `storage` event (other tab synced), + 60s tick.
- **UI:** "⟳ Sync now" button in the sidebar footer + a `.sync-last-label` status line (shows in desktop sidebar AND the mobile slide-out menu); "⟳ Sync" button in the mobile top bar. Both route through `triggerManualSync()` → `Sync.syncNow()`.
Sentinels S-123 (atomic writes schedule sync), S-124 (syncNow leader-runs/follower-delegates), S-125 (display-stamp never touches the pull cursor — data-loss guard), S-126 (confirmDraftItem durable). Mutations S-123/124/125/126 + S-127 (delivery branch, sentinel:S-123). Regression smoke 114/114 (no break); smoke 118/118; targeted 5 CAUGHT/0 BLIND/0 INFRA. Full sweep → audit-artifacts/WAVE-L2-SWEEP.log.

### L2 code-audit brief — GPT (paste as-is)
> BOB Stock App — Wave L2 (Tier 5: sync reliability + manual Sync) CODE audit. AUDIT ONLY — report findings, do NOT edit. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05` (db.js, phase2.js, sync.js, index.html). RUN the code (browser/Node vm), don't review statically. This implements the design YOU design-reviewed (REWORK → adopted); verify the build matches and stress the tricky parts:
> 1. **syncNow single-syncer invariant (your P1)** — `Sync.syncNow()` + `_runSyncCycle()` + the `request-sync` BroadcastChannel case. Confirm: a LEADER runs push→pull (queues one follow-up if mid-cycle, never overlaps); a FOLLOWER with a live leader DELEGATES (posts request-sync, does NOT call push/pull itself); no-live-leader → claims leadership then runs. Any path where two tabs push/pull concurrently? Any way `_syncing`/`_syncQueued` deadlocks or drops a request? Is the leader's `request-sync` refresh-then-cycle correct?
> 2. **bob_last_sync untouched (your P0 data-loss)** — `_stampUiSync()` writes ONLY `bob_ui_last_sync`; confirm NOTHING now writes `bob_last_sync` except the pull watermark path (sync.js ~946/1084). Grep both files.
> 3. **_afterLedgerWrite (#8)** — runs only after the Dexie txn completed + only on ok===true + try/catch (a sync-schedule throw must not fail a successful local write). On atomicTransferWriteDurable, atomicDeliveryWrite, addTransactionsDurable. Any double-scheduling concern, any re-entrancy?
> 4. **confirmDraftItem (#2)** — now async+durable; its submit-path caller no longer calls it per-item (marks in memory + single atomic write). Confirm no remaining caller fires it un-awaited in a loop, and the submit path still persists confirmed items correctly via Transfer.submitDraft.
> 5. **reconnect (#4)** — `online` fires syncNow debounced 2.5s, not on the initial call; flap-safe?
> Report P0–P3 with file:line. Verdict: PASS / sound-with-changes / BLOCK.

### L2 code-audit brief — Gemini (paste as-is; CLI, report-only)
> BOB Stock App — Wave L2 (Tier 5: sync reliability + manual Sync) CODE audit. AUDIT ONLY — report findings, do NOT edit any file (reviewer, not engineer). Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05` (db.js, phase2.js, sync.js, index.html). RUN the code (browser/Node vm). This implements the design you design-reviewed (REWORK → adopted). Verify and stress:
> 1. `Sync.syncNow()`: leader runs push→pull (one queued follow-up, no overlap); follower with a live leader delegates via BroadcastChannel `request-sync` and does NOT push/pull directly; no live leader → claims leadership then runs. No cross-tab double-push.
> 2. `_stampUiSync()` writes ONLY `bob_ui_last_sync`; `bob_last_sync` (pull cursor) is never overwritten by the UI stamp (your P0 — confirm via grep).
> 3. `DB._afterLedgerWrite()` on atomicTransferWriteDurable/atomicDeliveryWrite/addTransactionsDurable: success-gated, post-transaction, try/catch; transfer + delivery writes now schedule a sync (no stranded stock).
> 4. `confirmDraftItem` async+durable; the submit path marks confirmed in memory + single atomic write (no per-item concurrent durable writes — your HIGH concurrency finding).
> 5. reconnect (`online`) fires syncNow debounced 2.5s, not on the initial call.
> Report issues with file:line + severity. Verdict: PASS / approve-with-changes / BLOCK.

## WAVE L2 CODE AUDIT R1 (2026-06-19): GPT sound-with-changes (P2 + P3) — FIXED; Antigravity (Gemini) PASS (missed both)
Note: the "Gemini" auditor was rebranded **Antigravity** from this round on; same role (run-the-code, report-only reviewer). Older waves keep the "Gemini" label for history.

**GPT P2 (real — manual Sync could silently no-op):** `_runSyncCycle()` guarded only on `_syncing`, but a raw `push()`/`pull()` from the background `poll()` or the `scheduleSync` debounce holds `_syncLock` WITHOUT setting `_syncing`. A manual "Sync now" / reconnect cycle that collided with one of those would run, have its own push+pull both skip on `_syncLock`, and return `ok:true` having done nothing. GPT proved it with a live in-flight pull.
**FIXED (sync.js):** `_runSyncCycle` now queues when `_syncing || _syncLock`; new `_drainSyncQueue()` runs the ONE queued cycle the moment a raw push/pull releases the lock (called in push()'s and pull()'s `finally`, guarded `!_syncing` so an in-flight cycle's own releases don't re-enter — the do-while still drains mid-cycle requests). Manual/reconnect sync now always runs (deferred, never dropped).

**GPT P3 (cosmetic — redundant churn, GPT itself "not a blocker"):** `pull()` stores remote rows via `addTransactionsDurable()`, which now fired `_afterLedgerWrite()` → `scheduleSync()` even though those rows are already `_synced:true`; combined with pull's own `DB.commit()` that double-scheduled a push on every pull.
**FIXED (db.js + sync.js):** `addTransactionsDurable(txns, opts)` skips `_afterLedgerWrite()` when `opts.remote` is set; `pull()` passes `{ remote: true }`. Local bulk adds still schedule. Removes the L2-introduced extra schedule (the remaining single schedule via pull's pre-existing `DB.commit()` is prior behaviour, out of L2 scope — documented, not touched).

**Antigravity (Gemini) R1:** PASS, zero changes — missed BOTH P2 and P3 (recurring pattern: GPT is the edge-case/concurrency gate). It did run the full harness independently: verify-app 57 PASS, smoke 118/118, saboteur 127/127 — confirming the pre-fix build was harness-clean (the P2/P3 gaps were behavioural, not sentinel-covered until now).

Sentinels added: **S-128** (manual/reconnect sync collides with an in-flight raw push/pull → queues, not dropped, then runs push→pull on lock release) and **S-129** (remote pull-merge bulk add does NOT schedule a push; a local bulk add still does). Mutations S-128 (revert the `_runSyncCycle` guard) + S-129 (always schedule, ignoring `opts.remote`).
Smoke **120/120**; full sweep baseline 120/120, **129 CAUGHT / 0 BLIND / 0 SKIPPED / 0 INFRA-FAIL** → audit-artifacts/WAVE-L2r1-SWEEP.log. Harness now 120 sentinels / 129 mutations. NOT committed.

### L2r1 re-audit brief — GPT (paste as-is; confirm the P2 + P3 fixes)
> BOB Stock App — Wave L2 RE-AUDIT (your P2 + P3 fixes). AUDIT ONLY, report only. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`, files `sync.js` + `db.js`. RUN the code (browser/Node vm). Your two findings are fixed:
> **P2 (manual/reconnect sync silently no-ops when it collides with a raw poll/debounce push/pull holding `_syncLock` without `_syncing`):** `_runSyncCycle()` now queues when `this._syncing || this._syncLock`; a new `_drainSyncQueue()` (called in push()'s and pull()'s `finally` after `_syncLock` is released, guarded `!this._syncing`) runs the ONE queued cycle when the raw op finishes. Re-confirm: (1) a `syncNow()`/`request-sync`/reconnect that arrives while a raw poll/debounce push/pull is in flight is QUEUED (`_syncQueued=true`), not run inline, and does NOT report a successful sync having done nothing; (2) it RUNS exactly once (push→pull) the moment the lock releases; (3) no double-run, no lost request, no deadlock if `_syncQueued` is set with no raw op pending; (4) the in-flight-cycle case still drains mid-cycle requests via the do-while (the finally drain must NOT re-enter while `_syncing`); (5) the single-syncer invariant (leader-owned, follower delegates) is intact.
> **P3 (remote pull-merge double-schedules a push):** `addTransactionsDurable(txns, opts)` skips `_afterLedgerWrite()` when `opts.remote`; `pull()` calls it with `{ remote:true }`. Re-confirm: (1) a pull merge of remote (`_synced:true`) rows schedules NO push via `_afterLedgerWrite`; (2) a LOCAL bulk add (no opts) still schedules; (3) the transfer/delivery durable writes (atomicTransferWriteDurable/atomicDeliveryWrite) still schedule (unchanged); (4) no stranded-stock regression. Note the remaining single schedule via pull's pre-existing `DB.commit()` is prior behaviour, intentionally out of scope. Verdict: PASS / sound-with-changes / BLOCK, file:line.

### L2r1 re-audit brief — Antigravity (paste as-is; CLI, report-only)
> BOB Stock App — Wave L2 RE-AUDIT (two fixes from the GPT audit). AUDIT ONLY — report findings, do NOT edit any file (reviewer, not engineer). Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`, files `sync.js` + `db.js`. RUN the code (browser/Node vm). Verify and stress:
> 1. **Manual/reconnect sync vs in-flight raw push/pull:** `_runSyncCycle()` queues when `_syncing || _syncLock`; `_drainSyncQueue()` (in push()/pull() `finally`, guarded `!_syncing`) runs the queued cycle when the raw op releases the lock. Confirm a manual "Sync now"/reconnect that collides with a background poll is queued (not silently dropped) and then runs push→pull exactly once; no double-run, no lost request, no deadlock; the single-syncer invariant (leader runs, follower delegates) is intact.
> 2. **Pull-merge schedule:** `addTransactionsDurable(txns, opts)` skips the sync-schedule hook when `opts.remote`; `pull()` passes `{remote:true}`; a remote merge schedules no push, a local bulk add still does, and transfer/delivery durable writes still schedule (no stranded stock).
> Report issues with file:line + severity. Verdict: PASS / approve-with-changes / BLOCK.

## WAVE L2r1 RE-AUDIT (2026-06-19): GPT PASS + Antigravity PASS — BOTH CLEAN, zero changes. WAVE L2 CLOSED.
- **GPT re-audit:** PASS, no P0–P3. Verified file hashes, traced both fixes to exact lines, ran verify-app 57P/smoke 120/120 (incl. S-128/S-129), + custom Playwright probes for raw-pull collision, raw-push collision, live request-sync delegation under leader lock, reconnect-debounce collision, queued-with-no-raw-op drain, mid-cycle do-while drain, remote/local/transfer/delivery scheduling. Confirmed: collision → queued (not inline, not false-success) → drains once push→pull; mid-cycle handled by do-while, finally drain doesn't re-enter while _syncing; follower delegates; remote _synced rows skip _afterLedgerWrite while local/transfer/delivery still schedule (no stranded stock).
- **Antigravity re-audit:** PASS / Approved, zero changes. Independently confirmed both fixes (S-128/S-129) + smoke 120/120 + verify-release + csp-check. Report: code_re_audit_report.md.

**STATUS: Wave L1 (transfer domain integrity) + Wave L2 (sync reliability + manual Sync) both BUILT, harness-proven (120 sentinels / 129 mutations, 0 blind/skipped/infra), DUAL-AUDITED PASS, and COMMITTED 2026-06-19 as `1e5949d` on `fix/cli-tri-audit-2026-05` (NOT merged to main = NOT deployed).**
Remaining in the Wave L plan: **L3 — access / visibility / UX safety**. Still Azure-deferred (banked): #9 full cross-device cancel idempotency, #13 multi-table sync of transfer/delivery/stocktake records.

## WAVE L3 SCOPE (discovery 2026-06-19, verified against CURRENT post-L1/L2 code) — access / visibility / UX safety
Sole engineer Claude; design-review FIRST (GPT + Antigravity), then build -> sentinels -> saboteur -> code-audit -> commit on Kunal OK. (L2's pre-build design review caught 3 real flaws — same gate here.)

Re-verified per item (line numbers current):
- **#6 GPTa-26 franchisee multi-store transfer visibility — LIVE** (phase2.js:391-393): `list()` filters store-level non-mgmt users by a single `Auth.storeId()` (=storeIds[0], Auth at index.html:907). A franchisee (`isStoreLevel` true, `isMgmt` false; e.g. u_fran_c owns cockburn_office+cockburn) sees only ONE store's transfers.
- **#10 Ca-role `_canViewHistory` isAtLeast trap — LIVE** (phase2.js:48): `Auth.isAtLeast('store_manager') && !Auth.is('staff')`. Rank order (index.html:904) = staff < franchisee < store_manager < ... so a FRANCHISEE fails the check and can't view transfer history despite owning the stores. The exact isAtLeast-ordering trap the codebase warns about (use explicit role SETS for gates).
- **#11 Ca-M10 negative stock never floored/flagged — LIVE** (index.html:1072/1087 sum, 1128 `qty()`): per-txn qty floored >=0 on load (`_safeQty`), but the SUMMED `_qtyCache[store][prod]` can still go negative (OUTs>INs from stale/legacy data); `Stock.qty()` returns it raw; no display floor, no director alert.
- **#12 W8 logout transfer-draft leak — ALREADY FIXED (Wave G)** (index.html:5190 + phase2.js:1693-1707): `resetState` lives on `TransferUI`, logout calls it guarded, and it clears all _txState (create*, receive*, flag/draft maps, optEdits). The 2026-06-18 triage was stale. PROPOSE DROP — auditors to confirm completeness (incl. transient `_creating`).

**L3 build items = #6, #10, #11** (#12 confirm-and-drop).

### Proposed approach (to be design-reviewed, NOT yet built)
- **#6** — in `Transfer.list`, replace the single-store branch with an `Auth.storeIds()` inclusion filter, matching the hub UI which already uses storeIds. Sweep for any OTHER single-store `Auth.storeId()` reads in the transfer/stock views that hide multi-store data for a franchisee (createFrom default at phase2.js:928 using storeId() is a sensible default, not a visibility bug).
- **#10** — replace `isAtLeast('store_manager')` with an explicit allow SET {store_manager, franchisee, territory_manager, head_office, director} (everyone above staff). Keep `!Auth.is('staff')` semantics (staff = the shared store-computer account).
- **#11** — Kunal decision (locked): floor the DISPLAYED qty at 0 everywhere staff can see it (never show a negative number); surface a negative-stock alert to the DIRECTOR only; do NOT rewrite the underlying ledger/cache. Proposed shape: keep `Stock.qty()` RAW (reports/reorder/sell-through depend on the true sum); add a display-only floor for staff-facing screens; add a director-only negative-stock surfacing (which screen? = design question).

### Design questions for the auditors
1. **#11 placement** — floor at the display layer (keep `Stock.qty()` raw) vs floor inside `qty()`? Flooring in `qty()` would corrupt reorder/sell-through/value maths that need the true sum. Proposal: raw `qty()` + a display-floor helper + a separate director negative-stock signal. Agree? And WHERE should the director alert live — dashboard banner, a dedicated negative-stock report row, or both?
2. **#6 sweep** — beyond `Transfer.list`, other single-store `Auth.storeId()` reads that under/over-show a multi-store franchisee? Confirm the inclusion-filter shape doesn't break a single-store store_manager or leak across franchisees.
3. **#10 staff** — should the shared store-computer (staff) view transfer HISTORY at all? Current intent excludes staff; confirm vs Kunal's Wave B access matrix and that the explicit set is complete.
4. **#12** — confirm `TransferUI.resetState` + the logout call fully clear transfer state (incl. transient `_creating`); agree it's safe to drop.

### L3 design-review brief — GPT (paste as-is)
> BOB Stock App — Wave L3 (Tier 5: access / visibility / UX safety) DESIGN REVIEW — review the PLAN before I build it. ADVISE ONLY, do NOT edit. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05` (L1+L2 just committed as 1e5949d), files phase2.js + index.html. RUN/inspect the code to confirm the findings, then critique the plan. Three fixes:
> 1. Franchisee multi-store transfer visibility (GPTa-26): `Transfer.list` (phase2.js:391-393) filters store-level non-mgmt users by a single `Auth.storeId()` (=storeIds[0]); a franchisee owning multiple stores sees only one store's transfers. Plan: switch to an `Auth.storeIds()` inclusion filter. Confirm the bug + fix shape; sweep for OTHER single-store `Auth.storeId()` reads in the transfer/stock UI that under/over-show a multi-store franchisee.
> 2. `_canViewHistory` role trap (Ca-role): phase2.js:48 uses `Auth.isAtLeast('store_manager')`; rank order (index.html:904) puts franchisee BELOW store_manager, so franchisees are wrongly denied transfer history. Plan: explicit allow SET {store_manager, franchisee, territory_manager, head_office, director}. Confirm vs the role model; any other isAtLeast gates with the same franchisee-below-manager trap?
> 3. Negative stock display + director alert (Ca-M10, Kunal decision): summed `Stock.qty()` (index.html:1128; cache 1072/1087) can be negative; no display floor, no alert. Kunal's locked rule: floor the DISPLAYED qty at 0 for staff (never show negative), alert the DIRECTOR only, NEVER rewrite the ledger/cache. Plan: keep `Stock.qty()` RAW (reorder/sell-through/value need the true sum), add a display-only floor for staff-facing renders, add a director-only negative-stock signal. Critique: floor at display vs in qty()? Best place for the director alert (dashboard banner / report / both)? Any maths that already relies on qty() never being negative, or that would break if we floor in the wrong place?
> Also confirm #12 (logout transfer-draft leak) is ALREADY fixed: `TransferUI.resetState` (phase2.js:1693-1707) is called by logout (index.html:5190) and clears all _txState — safe to drop from L3? Report P0–P3 design concerns with file:line. Verdict: PLAN-SOUND / REWORK / BLOCK.

### L3 design-review brief — Antigravity (paste as-is; CLI, report-only)
> BOB Stock App — Wave L3 (Tier 5: access / visibility / UX safety) DESIGN REVIEW — review the PLAN before build. ADVISE ONLY, do NOT edit any file (reviewer, not engineer). Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05` (L1+L2 committed as 1e5949d), files phase2.js + index.html. RUN/inspect the code, confirm findings, critique the plan:
> 1. Franchisee multi-store transfer visibility: `Transfer.list` (phase2.js:391-393) uses single `Auth.storeId()`; plan = `Auth.storeIds()` inclusion filter. Confirm + sweep for other single-store reads that hide multi-store data.
> 2. `_canViewHistory` (phase2.js:48) uses `isAtLeast('store_manager')` — franchisee ranks below store_manager (index.html:904) so is wrongly denied; plan = explicit role set {store_manager,franchisee,territory_manager,head_office,director}. Confirm + flag any other isAtLeast gates with the same trap.
> 3. Negative stock (summed `Stock.qty()` index.html:1128 can be negative): Kunal's rule = floor DISPLAY at 0 for staff, alert director only, never rewrite the ledger. Plan = keep qty() raw, display-floor for staff screens, director-only negative-stock signal. Critique placement (display vs qty()), best home for the director alert, and any calc that wrongly assumes non-negative qty.
> Also confirm #12 (logout draft leak) is already fixed by `TransferUI.resetState` (phase2.js:1693-1707) called at index.html:5190 — safe to drop? Report issues with file:line + severity. Verdict: PLAN-SOUND / REWORK / BLOCK.

## WAVE L3 DESIGN REVIEW R1 — GPT: REWORK (no P0/P1; 3 scope additions + 1 routing pref). Antigravity pending.
GPT confirmed all 3 findings via browser probes, cleared #12 (drop), and expanded scope:
- **#6 confirmed** (phase2.js:390 single storeId; franchisee saw only first store) — fix shape (storeIds inclusion) correct.
- **#6b NEW — transfer CREATE UI over-shows stores** (phase2.js:928, 945): a 2-store franchisee gets all 11 From/To options incl. unauthorized stores. NOT a write bypass (Transfer.create rejects unauthorized at phase2.js:66) but a visibility/UX leak. Add scoped store-option generation.
- **#6c NEW — other single-store stock routes bind Auth.storeId()** (index.html:1890/1914/1931/1952, route at 5122): franchisee nav avoids them but they stay callable. Add a store picker/redirect for multi-store users, or guard the routes.
- **#10 confirmed**; no other same-class isAtLeast trap (_canReceive phase2.js:44 is staff-threshold, safe). PREF: add a central cap beside Auth._caps/Auth.can (index.html:921) instead of another local ad-hoc role list (aligns with the banked R10-L30-1 drift TODO — route transfer gates through Auth.can).
- **#11 confirmed -7 raw**; UI.stockBadge() (index.html:1340) renders -7. MUST keep flooring OUT of Stock.qty() (index.html:1128) AND out of global UI.stockBadge() — director/HO/report surfaces use them. Raw negative is consumed by value/report math (index.html:1168, 2780, 3594) + dashboard/reorder (3678). → Floor ONLY at staff/non-director display call sites; director alert = dashboard banner + drill-down list/report; never notify staff/franchisee.
- **#12 CLEARED** — safe to drop (runtime-confirmed resetState empties all _txState buckets).
GPT ran verify-app 57 PASS. REVISED PLAN to be finalised after Antigravity converges (then build).

## Kunal decisions for L3 (locked 2026-06-19)
- **Staff + transfer history (#10):** NO — the shared store-computer (staff) does NOT see transfer history. Keep staff excluded; explicit allow set = {store_manager, franchisee, territory_manager, head_office, director}.
- **#6c single-store stock routes (index.html:1890/1914/1931/1952, route 5122):** BLOCK them for multi-store users (simpler + safe; the franchisee multi-store pages already cover the need) — do NOT build a store-picker for now.

## WAVE L3 DESIGN REVIEW R2 — Antigravity: PLAN-SOUND (with adjustments). CONVERGES with GPT.
- **#6 confirmed** (Transfer.list single-store) — storeIds() fix correct. Also flags **Store Stock + Store History pages restrict franchisees to one store** (= GPT's #6c; Kunal decision: BLOCK for multi-store users).
- **#10 confirmed** — advises the centralized `Auth._caps` map over a hardcoded role set (= GPT's pref). ADOPTED.
- **#11 confirmed** `Stock.qty()` stays raw, floor at display. TWO new specifics:
  - **stockBadge bug:** `UI.stockBadge()` renders a NEGATIVE qty as amber "LOW" — should be red "OUT". Fix as part of #11.
  - **valuation bug (NEW, needs Kunal):** `totalValue` + `_stockOnHandCost` mistakenly SUBTRACT negative stock → a counting glitch understates the company's inventory $ value. Antigravity says negatives shouldn't drag asset value down.
- **#12 CLEARED** — already fully resolved (resetState in logout). Drop confirmed by both.

### CONVERGENCE: both PLAN-SOUND-with-additions, zero conflicts. One OPEN Kunal decision before build:
**Negative stock in the money math.** GPT: keep `Stock.qty()` raw; floor only at staff display. Antigravity: ALSO, asset valuation (`totalValue`/`_stockOnHandCost`) currently lets a phantom -7 subtract real dollars from inventory value. DECISION NEEDED: in the $ valuation, should a negative on-hand contribute its true negative value (raw) or be floored to $0 (a counting error = $0 of asset, surfaced via the director alert instead)? Reorder/sell-through stay raw regardless (a negative is a real shortage signal). Claude recommends FLOOR-TO-$0 in valuation (a counting glitch shouldn't understate reported inventory value; the director alert is the channel that surfaces it). AWAITING KUNAL.

## WAVE L3 — FINAL PLAN (decisions locked 2026-06-19, both auditors converged → BUILD)
Kunal decision: **valuation = Option A** — a negative on-hand contributes $0 to inventory value (not negative dollars); director alert surfaces it. Reorder/sell-through keep raw.

Build items:
- **#6 list** — `Transfer.list` (phase2.js): store-level non-mgmt users filter by `Auth.storeIds()` inclusion (from OR to), HO/director unchanged.
- **#6b create UI** — `TransferUI` create From/To option generation (phase2.js ~928/945): scope options to the user's `storeIds()` for store-level non-mgmt; HO/director see all. (Domain already rejects unauthorized at phase2.js:66 — this is the visibility half.)
- **#6c block routes** — the single-store stock routes (index.html:1890/1914/1931/1952, dispatch ~5122): BLOCK/redirect for multi-store users (Kunal: no picker).
- **#10 history gate** — route `_canViewHistory` through the central `Auth._caps`/`Auth.can` map (index.html ~921); allow set {store_manager, franchisee, territory_manager, head_office, director}; staff excluded (Kunal).
- **#11 negative stock** — (a) keep `Stock.qty()` RAW; (b) floor to 0 at staff/non-director display call sites; (c) `UI.stockBadge()`: a negative renders red "OUT" (floored to 0 for staff), not amber "LOW"; (d) director-only surfacing = dashboard banner + drill-down negative-stock list; (e) valuation `totalValue`/`_stockOnHandCost` floor negative contribution to $0 (Option A); (f) reorder/sell-through stay raw.
- **#12 DROPPED** (already fixed, both auditors confirmed).

Each sub-step: discover → build → sentinels → saboteur (targeted + full sweep 0-blind) → GPT+Antigravity code-audit → commit on Kunal OK. Harness baseline entering L3: 120 sentinels / 129 mutations.

## WAVE L3 BUILT (2026-06-20, NOT committed)
- **#10 (phase2.js):** `_canViewHistory` → `Auth.can('viewTransferHistory')`; new cap added to the central `Auth._caps` map (index.html) = {store_manager, franchisee, territory_manager, head_office, director}. Staff excluded (Kunal). Fixes the isAtLeast-trap that denied franchisees.
- **#6 (phase2.js):** `Transfer.list` store-level non-mgmt branch now filters by `Auth.storeIds()` inclusion (was single `Auth.storeId()`) — a multi-store franchisee sees all their stores' transfers.
- **#6b (phase2.js):** `renderCreateTransfer` From/To options scoped via `_visibleStores` to the user's owned stores (HO/director see all) — mirrors the domain reject; no unauthorized store offered.
- **#6c (index.html):** new `Pages._requiresSingleStore(elId)` guard on `storeStock()` + `storeHistory()` — a multi-store user (storeIds.length !== 1) is blocked with a "Multiple stores → use Comparison/Alerts/Reports" notice (Kunal: block, no picker).
- **#11 (index.html):** (a) `Stock.qty()` UNCHANGED (raw). (b) `UI.stockBadge` floors a negative to 0 and shows red "OUT" (was amber "LOW"; `q===0` valid post-floor). (c) staff log-movement "current stock" floored (`Math.max(0,...)`). (d) `Stock.totalValue` + `Pages._stockOnHandCost` floor negative contribution to $0 (Option A). (e) new `Stock.negativeStock(storeIds)` (raw) + `Pages._negativeStockBanner` shown DIRECTOR-ONLY at the top of `hoStock` (Stock Overview). Reorder/sell-through untouched (raw).
- **#12 DROPPED** (already fixed Wave G, both auditors confirmed).
Sentinels **S-130** (_canViewHistory cap), **S-131** (list multi-store), **S-132** (create-UI scoping), **S-133** (#6c block), **S-134** (badge floor/OUT), **S-135** (totalValue Option A), **S-136** (negativeStock + director banner). One mutation each (S-130..S-136).
Smoke **127/127**; full sweep baseline 127/127, **136 CAUGHT / 0 BLIND / 0 SKIPPED / 0 INFRA-FAIL** → audit-artifacts/WAVE-L3-SWEEP.log. Harness now **127 sentinels / 136 mutations**. NOT committed.

### L3 code-audit brief — GPT (paste as-is)
> BOB Stock App — Wave L3 (Tier 5: access / visibility / UX safety) CODE audit. AUDIT ONLY — report findings, do NOT edit. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05` (L1+L2 committed 1e5949d; L3 uncommitted in the working tree), files phase2.js + index.html. RUN the code (browser/Node vm), don't review statically. This builds the plan you DESIGN-REVIEWED (REWORK → adopted, incl. your #6b/#6c additions + central-cap routing). Verify the build matches and stress it:
> 1. **#10 history cap** — `_canViewHistory` now returns `Auth.can('viewTransferHistory')`; the cap set in `Auth._caps` = {store_manager,franchisee,territory_manager,head_office,director}. Confirm a franchisee CAN view history, staff CANNOT, and no other isAtLeast role-trap remains for this gate.
> 2. **#6 list** — `Transfer.list` filters store-level non-mgmt users by `Auth.storeIds()` inclusion. Confirm a multi-store franchisee sees ALL their stores' transfers, a single-store manager is unaffected, and HO/director still see everything; no cross-franchisee leak.
> 3. **#6b create UI** — `renderCreateTransfer` scopes From/To options to owned stores (`_visibleStores`); HO/director see all. Confirm no unauthorized store is offered and this matches the domain reject at Transfer.create (phase2.js:66); a franchisee can still pick among their own stores.
> 4. **#6c block** — `Pages._requiresSingleStore` blocks `storeStock`/`storeHistory` for a user whose storeIds.length !== 1. Confirm a multi-store franchisee is blocked (no single-store data rendered), a single-store manager renders normally, and there's no path that still renders the single-store view for a multi-store user.
> 5. **#11 negative stock (Kunal: floor display, director alert, NEVER rewrite ledger; valuation Option A)** — confirm: `Stock.qty()` stays RAW; `UI.stockBadge` floors a negative to "0 — OUT" (red, not amber LOW) and never prints a negative; the staff log-movement current-stock is floored; `Stock.totalValue` + `_stockOnHandCost` floor a negative on-hand to a $0 contribution (never subtract); `Stock.negativeStock` reports raw negatives and `_negativeStockBanner` shows DIRECTOR-ONLY; reorder/sell-through still use raw qty. Any staff-facing surface that still leaks a negative number? Any calc that now wrongly floors where it needed raw (or vice-versa)?
> Report P0–P3 with file:line. Verdict: PASS / sound-with-changes / BLOCK.

### L3 code-audit brief — Antigravity (paste as-is; CLI, report-only)
> BOB Stock App — Wave L3 (Tier 5: access / visibility / UX safety) CODE audit. AUDIT ONLY — report findings, do NOT edit any file (reviewer, not engineer). Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05` (L1+L2 committed 1e5949d; L3 uncommitted), files phase2.js + index.html. RUN the code (browser/Node vm). This builds the plan you design-reviewed (PLAN-SOUND → adopted). Verify and stress:
> 1. `_canViewHistory` → central `Auth.can('viewTransferHistory')` cap {store_manager,franchisee,territory_manager,head_office,director}; franchisee CAN view, staff CANNOT.
> 2. `Transfer.list` filters store-level non-mgmt by `Auth.storeIds()` inclusion — multi-store franchisee sees all their stores; single-store manager + HO/director unaffected; no cross-franchisee leak.
> 3. `renderCreateTransfer` scopes From/To options to owned stores; no unauthorized store offered (matches the Transfer.create domain reject).
> 4. `Pages._requiresSingleStore` blocks `storeStock`/`storeHistory` for storeIds.length !== 1 (multi-store franchisee blocked; single-store manager normal).
> 5. Negative stock: `Stock.qty()` raw; `UI.stockBadge` floors negative → red "0 — OUT" (you flagged the amber-LOW bug); log-movement current-stock floored; `totalValue`/`_stockOnHandCost` floor negative to $0 (Option A — you flagged the valuation subtract); `negativeStock`+`_negativeStockBanner` DIRECTOR-ONLY; reorder/sell-through raw. Any staff surface still leaking a negative? Any calc floored that needed raw?
> Report issues with file:line + severity. Verdict: PASS / approve-with-changes / BLOCK.

## WAVE L3 CODE AUDIT R1 (2026-06-20): GPT BLOCK + Antigravity BLOCK — both CONVERGED on 2 real defects. FIXED in L3r1.
Both auditors ran the built code and BLOCKED — correctly. #6 (list), #6b (create UI), #6c (block), #10 cap, #12 drop, director banner, totalValue/_stockOnHandCost all PASSED. Two defects:
- **(#10 UNENFORCED IN UI — Antigravity HIGH, the miss):** `_canViewHistory` was fixed in the DOMAIN but the UI never called it — `buildSidebar`/`renderTransfersHub`/`openDetail` let STAFF reach the hub, see the Completed tab, and open completed transfer history. (Domain helper correct; gate not wired.)
- **(#11 INCOMPLETE SWEEP — both, the big one):** `UI.stockBadge`/`totalValue`/`_stockOnHandCost`/`negativeStock` were floored, but ~12 OTHER render/export/calc surfaces still printed raw negatives or subtracted negative value: dashboard Top Alerts (3781), Low Stock Alerts detail (2309) + copy summary (2334/2339), Stock Overview stat recalc (calcVal 2440) + per-row val/status (2474/2475) + multi-store total (2481), reports overview value (2828) + category total (2836) + snapshot total (2875) + category stockNow (2885), stock CSV (2939), store-stock progress width/class (1954). Plus `Stock.alerts` classified a negative as amber LOW (critical:qty===0) instead of red OUT. ROOT CAUSE = the P-12/P-17 incomplete-sweep trap; my sentinels tested the HELPERS, not the render SURFACES, so they stayed green while surfaces leaked. LESSON RE-BANKED: surface-leak fixes need sentinels that drive the real render surface.

### WAVE L3r1 BUILT (2026-06-20, NOT committed) — both defects, whole-population
- **#10 UI gating (phase2.js):** `renderTransfersHub` computes `canViewHistory=Auth.can('viewTransferHistory')`; excludes completed transfers from the list and drops the Completed tab when false; `openDetail` blocks a completed transfer for non-history users (staff still RECEIVE in-transit transfers — only completed *history* is gated).
- **#11 whole-surface sweep (index.html):** new central `Stock.dispQty(pid,sid)=Math.max(0,qty)`; ALL non-director value totals routed through it (calcVal, report overview value, category total, category stockNow, stock CSV, `_stockOnHandCost`); display numbers floored at every staff-facing surface (dashboard Top Alerts, Low Stock Alerts detail + copy summary, Stock Overview multi-store total, store-stock progress width/class); per-row val/status floored (Stock Overview storeFilter table); `Stock.alerts` `critical:qty<=0` so a negative is OUT (red) not LOW (amber). `Stock.qty()` still RAW; reorder + sell-through still RAW; director banner still raw + director-only.
Sentinels — **SURFACE-driving** (the fix for the lesson): **S-137** (hub hides Completed tab + completed rows from staff via live renderTransfersHub), **S-138** (openDetail blocks completed for staff, allows director), **S-139** (negative = critical OUT + Low Stock Alerts page shows no negative number via live hoAlerts), **S-140** (Stock Overview table prints no negative total via live hoStock), **S-141** (central dispQty floors). Mutations S-137 + S-137b(tab) + S-138 + S-139 + S-139b(detail floor) + S-140 + S-141.
Smoke **132/132**; full sweep baseline 132/132, **143 CAUGHT / 0 BLIND / 0 SKIPPED / 0 INFRA-FAIL** → audit-artifacts/WAVE-L3r1-SWEEP.log. Harness now **132 sentinels / 143 mutations**. NOT committed.
Coverage note (no silent cap): the #11 value sites all route through `dispQty` (S-141) or `totalValue` (S-135); display surfaces are driven by S-137/S-139/S-140 + the badge primitive (S-134). A few display echoes (alert copy-summary text, snapshot-tab total, store-stock progress width) share the same floor pattern but are not each individually mutation-guarded — verified by code-audit, flagged here for the re-audit to confirm.

### L3r1 re-audit brief — GPT (paste as-is; confirm both BLOCK fixes + check for any remaining leak)
> BOB Stock App — Wave L3 RE-AUDIT (your BLOCK: #10 unenforced UI gate + #11 incomplete negative-stock sweep). AUDIT ONLY, report only. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`, files phase2.js + index.html. RUN the code (browser/Node vm). Both are fixed:
> **#10 (your unenforced-gate BLOCK):** `renderTransfersHub` now computes `canViewHistory=Auth.can('viewTransferHistory')`, excludes completed transfers + drops the Completed tab when false; `openDetail` blocks a completed transfer for non-history users. Staff retain in-transit RECEIVE access. Re-confirm: a STAFF user cannot see the Completed tab, cannot see completed transfers in any hub tab, and cannot openDetail a completed transfer; a manager/director still can; staff can still receive an in-transit transfer.
> **#11 (your incomplete-sweep BLOCK):** new central `Stock.dispQty=Math.max(0,qty)`; every non-director VALUE total routes through it (calcVal/report overview value/category total/category stockNow/stock CSV/_stockOnHandCost); every staff-facing DISPLAY number is floored (dashboard Top Alerts, Low Stock Alerts detail + copy summary, Stock Overview multi-store total + per-row val/status, store-stock progress width); `Stock.alerts` now `critical:qty<=0` (negative = OUT, red). `Stock.qty()` stays RAW; reorder + sell-through stay RAW; the director banner stays raw + director-only. Drive a real outbound movement to force a raw negative, then SWEEP every non-director surface (dashboards, alerts, stock overview, reports overview/snapshot/category, CSV export, store stock) and confirm NONE prints a negative number or a negative $ value, and that the alert is red OUT not amber LOW. Confirm reorder/sell-through/director-banner still show the true (raw) values. Any surface STILL leaking a negative? Any place now flooring where raw was required? Verdict: PASS / sound-with-changes / BLOCK, file:line.

### L3r1 re-audit brief — Antigravity (paste as-is; CLI, report-only)
> BOB Stock App — Wave L3 RE-AUDIT (the two BLOCK defects). AUDIT ONLY — report findings, do NOT edit any file. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`, files phase2.js + index.html. RUN the code (browser/Node vm). Verify:
> 1. **#10 history gate now ENFORCED in the UI:** `renderTransfersHub` (canViewHistory → no Completed tab, completed rows excluded) + `openDetail` (blocks completed for non-history users). A STAFF user: no Completed tab, no completed rows, openDetail on a completed transfer blocked; can still RECEIVE in-transit. Manager/director unaffected.
> 2. **#11 whole-surface negative-stock sweep:** central `Stock.dispQty`; all non-director value totals (calcVal, report value, category/snapshot totals, stock CSV, on-hand cost) floor negatives to $0; all staff-facing display numbers floored (dashboard, Low Stock Alerts detail + summary, Stock Overview total + row val/status, store-stock progress); `Stock.alerts critical:qty<=0` (negative = red OUT). `Stock.qty()` raw; reorder + sell-through raw; director banner raw + director-only. Force a raw negative and sweep EVERY non-director surface — confirm no negative number or negative $ leaks anywhere, classification is OUT not LOW, and reorder/sell-through/director-banner keep the true values.
> Report issues with file:line + severity. Verdict: PASS / approve-with-changes / BLOCK.

## WAVE L3r1 RE-AUDIT (2026-06-20): GPT BLOCK (1 P1, real) + Antigravity PASS (missed it). FIXED in L3r2.
Fresh run (new line numbers confirm current tree). GPT re-confirmed #10 + every #11 visible surface PASS, but found ONE real P1:
- **DUPLICATE `_exportStockCSV` (GPT P1):** index.html had TWO `_exportStockCSV` definitions — the L3r1 fix landed on the FIRST (dead) one; the LIVE one (later def wins) still used raw `Stock.qty()` + raw total AND exported every active store's column + HO to a franchisee (negative-leak + cross-store visibility footgun). Reached via the reports "Current Stock CSV" button. Same duplicate-definition class as the historic `_downloadCSV` dup.
- **Antigravity:** PASS — verified surfaces/sentinels but did NOT catch the duplicate (GPT remains the edge-case gate). Pattern holds.

### WAVE L3r2 BUILT (2026-06-20, NOT committed)
- Fixed the **LIVE** `Pages._exportStockCSV` (index.html ~3265): store columns floored via `Stock.dispQty` + total floored (Option A); columns SCOPED to the user's own stores for franchisee/store-level (`myIds`), HO column only for non-franchisee (`showHO=isHO||!isFran`). No raw negative, no cross-store leak.
- **DELETED** the dead duplicate `_exportStockCSV` entirely — single source of truth now (kills the footgun that let r1 fix the wrong copy). Confirmed exactly one definition remains (grep).
Sentinel **S-142** drives the LIVE `_exportStockCSV` as a franchisee with a forced negative: asserts no negative cell/total AND columns scoped to own stores (no other store, no HO). Mutations S-142 (floor revert) + S-142b (scope revert).
Smoke **133/133**; full sweep baseline 133/133, **145 CAUGHT / 0 BLIND / 0 SKIPPED / 0 INFRA-FAIL** → audit-artifacts/WAVE-L3r2-SWEEP.log. Harness now **133 sentinels / 145 mutations**. NOT committed.
LESSON re-banked: duplicate method definitions in index.html (last-wins) mean a fix can land on a dead copy — after editing a Pages/Stock method, grep for duplicate definitions and fix/remove the LIVE one. (Same class as the `_downloadCSV` dup.)

### L3r2 re-audit brief — GPT (paste as-is; confirm the CSV P1 fix)
> BOB Stock App — Wave L3 RE-AUDIT #2 (your duplicate `_exportStockCSV` P1). AUDIT ONLY, report only. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`, file index.html. RUN the code. Your P1 (two `_exportStockCSV` defs — the L3r1 floor landed on the dead one; the LIVE one exported raw negative qty + raw total AND every store's column + HO to a franchisee) is fixed: the dead duplicate is DELETED (confirm only ONE `_exportStockCSV` definition exists), and the live one now (1) floors every store qty + the total via `Stock.dispQty` (Option A), (2) scopes columns to the user's own stores for franchisee/store-level (`myIds`), with the HO column only for non-franchisee. Re-confirm via the reports "Current Stock CSV" button as a FRANCHISEE with a forced raw negative: no negative number/total in the export, no other-store column, no HO column; as a director: all stores + HO, floored. Also re-confirm nothing else regressed in #10/#11 from this change. Any other duplicate-definition shadowing (e.g. another export/render method) still live? Verdict: PASS / sound-with-changes / BLOCK, file:line.

### L3r2 re-audit brief — Antigravity (paste as-is; CLI, report-only)
> BOB Stock App — Wave L3 RE-AUDIT #2 (duplicate `_exportStockCSV` P1 from the GPT re-audit). AUDIT ONLY — report findings, do NOT edit. Repo `C:\Users\joshi\repos\bob-stock`, branch `fix/cli-tri-audit-2026-05`, file index.html. RUN the code. The fix: the dead duplicate `_exportStockCSV` is deleted (confirm ONE definition remains) and the live one floors all qtys + total via `Stock.dispQty` and scopes columns to the user's own stores (HO column only for non-franchisee). Verify as a franchisee with a forced negative: the Current-Stock CSV shows no negative number/total and only the franchisee's own store columns (no other store, no HO); as a director: all stores + HO, floored. Confirm no #10/#11 regression. Check for any other live duplicate-definition shadowing among Pages export/render methods. Verdict: PASS / approve-with-changes / BLOCK, file:line.
